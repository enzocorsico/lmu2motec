export component AppHeader inherits VerticalLayout {
    spacing: 4px;

    Text {
        text: "LMU → MoTeC";
        color: #f2f6fa;
        font-size: 28px;
        font-weight: 700;
    }

    Text {
        text: "Convert your DuckDB telemetry files to MoTeC LD files.";
        color: #91a3b5;
        font-size: 14px;
    }
}
